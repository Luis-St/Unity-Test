#pragma kernel CSMain

RWTexture2D<float4> Result;
float Width;
float Height;

[numthreads(8,8,1)]
void CSMain(uint3 id : SV_DispatchThreadID) {
	float value = id.y / Height;
	Result[id.xy] = float4(value, value, value, 0.0);
}
